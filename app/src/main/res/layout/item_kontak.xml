<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="15dp">

    <ImageView
        android:id="@+id/iv_item_kontak"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        app:layout_constraintWidth_percent="0.1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/tv_item_sales_detail_payment"
        android:src="@drawable/ic_launcher_foreground"/>

    <TextView
        android:id="@+id/tv_item_sales_detail_payment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/iv_item_kontak"
        app:layout_constraintBottom_toBottomOf="@+id/iv_item_kontak"
        app:layout_constraintStart_toEndOf="@id/iv_item_kontak"
        android:layout_marginStart="18dp"
        android:text="@string/empty"
        android:textSize="16dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>